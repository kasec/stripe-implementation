# Stripe Implementation with react and express

Running project with following sentences:

```bash
stripe-implementation> yarn start
```

> Create `.env` file in **server** dir. with `.env.example` *variables*